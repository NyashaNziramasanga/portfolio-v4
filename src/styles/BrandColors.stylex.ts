import * as stylex from "@stylexjs/stylex";

/** Third-party brand colors that must not follow the portfolio theme. */
export const brandColors = stylex.defineConsts({
  /** #FF0000 */
  youtube: "#FF0000",
  /** #003B71 */
  flinders: "#003B71",
});
